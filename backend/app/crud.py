import sqlite3

#def add_item_to_db(db: sqlite3.Connection, name: str, category: str, image_name: str):
#    cursor = db.cursor()
#
#    query_category = """SELECT id FROM categories WHERE name LIKE ?"""
#    cursor.execute(query_category, (category,))
#    row = cursor.fetchone()
#   .
#   .


#上のコードは例。データベース操作の関数を書くファイル。